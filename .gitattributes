# Git LFS configuration for large files

# Audio files
*.wav filter=lfs diff=lfs merge=lfs -text

# Model files
models/*.h5 filter=lfs diff=lfs merge=lfs -text

# Video files
assets/*.mp4 filter=lfs diff=lfs merge=lfs -text

# Large data files
data/raw/**/*.wav filter=lfs diff=lfs merge=lfs -text
data/processed/**/*.joblib filter=lfs diff=lfs merge=lfs -text
